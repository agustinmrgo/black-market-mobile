import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: 'gray',
    borderRadius: 5,
    padding: 12,
    marginVertical: 10,
    width: '80%',
    radius: 8,
  },
});

export default styles;
